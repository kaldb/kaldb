#!/usr/bin/env bash
set -eu


#
#
#echo "astra"
#echo "warmup"
#for file in queries/* ; do
#    echo "$file"
#    time ./query_astra.sh "$file" | wc -c #jq '.responses[].hits.hits | length'
#done
#echo "benchmark"
#for file in queries/* ; do
#    echo "$file"
#    time ./query_astra.sh "$file" | wc -c #jq '.responses[].hits.hits | length'
#done
#
#echo "OS"
#echo "warmup"
#for file in queries/* ; do
#    echo "$file"
#    time ./query_os.sh "$file" | wc -c #jq '.responses[].hits.hits | length'
#done
#echo "benchmark"
#for file in queries/* ; do
#    echo "$file"
#    time ./query_os.sh "$file" | wc -c #jq '.responses[].hits.hits | length'
#done

